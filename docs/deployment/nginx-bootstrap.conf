# Replace verity.example.com. This site serves only certificate challenges.
server {
    listen 80;
    server_name verity.example.com;
    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }
    location / {
        return 503;
    }
}
